export interface TimeLeft {
  days: number;
  hours: number;
  minutes: number;
  seconds: number;
}

export interface TargetDate {
  day: string | null;
  month: string | null;
  year: string | null;
}
